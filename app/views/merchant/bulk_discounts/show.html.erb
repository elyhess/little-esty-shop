<h3 class="has-text-centered"><%= @bulk_discount.name %></h3>

<section class="section">
  <p class="is-size-4">Quantity Threshold: <%= @bulk_discount.quantity_threshold %></p>
  <p class="is-size-4">Discount Amount: <%= number_to_percentage(@bulk_discount.percentage, strip_insignificant_zeros: true) %></p>
</section>

<%= render partial: "back_button" %>